import type { NextConfig } from "next";

const nextConfig: NextConfig = {
    images: {
        domains: ['www.google.com','www.litespeedtech.com'], // replace with actual domain like "res.cloudinary.com"
    },};

export default nextConfig;
