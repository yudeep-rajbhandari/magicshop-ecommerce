import type { NextConfig } from "next";

const nextConfig: NextConfig = {
    images: {
        domains: ['ljzsiekkkrmgxocgbsou.supabase.co'], // replace with actual domain like "res.cloudinary.com"
    },};

export default nextConfig;
